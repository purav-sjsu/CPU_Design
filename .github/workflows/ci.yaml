name: CPU Design Tests

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Build and run utils tests
        run: |
          g++ -std=c++17 -o test_utils tests/test_utils.cpp utils/utils.cpp
          ./test_utils

      - name: Build and run gates tests
        run: |
          g++ -std=c++17 -o test_gates tests/test_gates.cpp datapath/gates.cpp
          ./test_gates

      - name: Build and run adders tests
        run: |
          g++ -std=c++17 -o test_adders tests/alu/test_adders.cpp datapath/adders.cpp utils/utils.cpp
          ./test_adders

      - name: Build and run bitwise tests
        run: |
          g++ -std=c++17 -o test_bitwise tests/alu/test_bitwise.cpp datapath/bitwise.cpp utils/utils.cpp
          ./test_bitwise